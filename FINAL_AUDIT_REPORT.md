# eDorm — Final Audit Report
_Generated: 2026-06-18_

> **Audit method note.** This audit was performed primarily by **static code analysis**
> plus execution of the project's **automated test scripts**. Items that require a live
> server + database + browser session to verify behaviorally are explicitly marked
> **`🔵 NEEDS-LIVE`** (logic verified in code, runtime behavior not exercised in this pass).
> Nothing is reported as PASS unless it was either verified in code or exercised by a test.

## Audit Summary
| Category | Total Checks | Passed | Failed | Warnings | Needs-Live |
|----------|-------------|--------|--------|----------|-----------|
| Functional Flows | _tbd_ | | | | |
| UI/UX Desktop | _tbd_ | | | | |
| UI/UX Mobile | _tbd_ | | | | |
| Codebase Cleanup | _tbd_ | | | | |

---

## Phase 3 — Codebase Cleanup (identification done first per execution order)

### 3.1 — Junk / Dead Files Identified

#### Untracked runtime artifacts (gitignored, safe to delete from disk)
| File / Dir | Size | Reason |
|------|------|--------|
| `logs/` (31 files) | ~120 MB | Rotated runtime logs (`combined*.log`, `audit.log`, etc.); gitignored |
| `server.log` | 64 KB | Runtime log; gitignored, regenerated on run |
| `C:UsersducquAppDataLocalTempserver-boot.log` | 25 KB | Malformed temp path written into project root (accidental literal Windows path) |
| `C:UsersducquAppDataLocalTempserver-verify.log` | 130 KB | Same — accidental literal temp path |

#### Tracked generated/one-off artifacts (candidates — see "Deletion decision" note)
| File | Reason | Referenced by |
|------|--------|---------------|
| `server.err` | Tracked error-log artifact (717 B) | none |
| `simulation-test-report.txt` | Regenerated by `scripts/run-simulation-e2e-test.js` | only its generator |
| `final-report.md` | Prior generated audit report | only `scripts/generate-final-report.js` |
| `final-report-v3.md` | Prior generated audit report | only `scripts/generate-final-report.js` |
| `i18n-verify.js` | Root one-off verification script, not in package.json scripts | none |
| `CLAUDE_MOBILE_PROMPT.md` | 51 KB prompt/spec doc | none — **flagged, not auto-deleted** |
| `EDORM_ANDROID_REPORT.md` | Android build report (modified in working tree) | none — **flagged, not auto-deleted** |

> **Deletion decision pending user confirmation** — see "Issues Requiring Manual Follow-up".
> The `.md`/report files sit adjacent to active thesis material and some are in the working
> tree; per the task's "when in doubt, flag — do not delete" rule these are not removed
> automatically.

#### Orphan EJS views (no route renders them)
| View | Reason |
|------|--------|
| `views/2fa-login.ejs` | Duplicate of `views/auth/2fa-login.ejs` (the live one); root copy never rendered |
| `views/admin/allocation/simulation-panel.ejs` | No reference anywhere (render or include) |
| `views/admin/forced-exit-candidates.ejs` | Route `/admin/forced-exit-candidates` returns **JSON**, never renders this view |
| `views/admin/planning/policy-suggestion-dashboard.ejs` | No reference anywhere |
| `views/shared/_workflow-progress.ejs` | Partial never `include()`-d |
| `views/student/enhanced-application-form.ejs` | No route renders it |
| `views/student/my-ranking.ejs` | Route `/student/my-ranking` returns **JSON**, never renders this view |
| `views/student/explore-rooms.ejs` | **No route at all** — yet linked from `my-ranking.ejs` & `room-360-viewer.ejs` → broken link (also a UX bug, see 2.4) |

#### Unmounted route files
None. All 45 route files under `src/routes/` are required/mounted (mobile sub-routes are
wired via relative requires in `src/routes/student/mobile/index.js`).

#### Orphan test/debug scripts in `scripts/`
`scripts/` contains many `seed-*`, `reset-*`, `fix-*`, `test-*` one-off CLI utilities.
Of the `test-*`/`e2e-*` named ones: `test-forecast-service.js` (npm `test:forecast`),
`test-new-features.js` (npm `test:features`), and `run-simulation-e2e-test.js` are
referenced/meaningful. `e2e-test.js` is a Playwright-style script not wired to package.json
— **flagged, not deleted** (may be intentional QA tooling).

### 3.2 — Console & Debug Cleanup
- **App code is clean.** Only 15 `console.log` calls exist across `src/` + `index.js`:
  - `src/migrations/updateStudentSchema.js` (11) — intentional CLI migration output.
  - `src/config/config.js` (2) — DB connect/disconnect, run before logger init. Acceptable.
  - `src/config/validateEnv.js` (1) — startup validation message, before logger init. Acceptable.
  - `src/services/roomViewerService.js:171` — inside a **browser-side `<script>` template
    literal** (`psv.on('hotspot-click', ...)`), i.e. client console output, not a server log.
- **No credential leaks** — no `console.*` prints of password/secret/token found.
- `scripts/` CLI utilities use `console.*` intentionally (correct for CLI output).

**Conclusion: no server-side debug `console.log` artifacts require removal.**

### 3.3 — Dead Code Flagged (Manual Review)
- 🔴 **BUG (not just dead code): `logSecurityEvent` is undefined in `index.js`.** It is
  called at `index.js:233` (emergency 2FA reset) and `index.js:263` (violations migration)
  but the require at `index.js:28` imports only `{ logger }`. Calling either endpoint throws
  `ReferenceError: logSecurityEvent is not defined`. **Fix:** change line 28 to
  `const { logger, logSecurityEvent } = require('./src/config/logger');`
  (Impacts Phase 1.10 emergency 2FA reset — see Phase 1.)

_(more dead-code findings appended after Phase 1)_

---

## Phase 1 — Functional Flows

**Legend:** ✅ verified (code/test) · 🔵 NEEDS-LIVE (logic verified, runtime not exercised) · ⚠️ warning · ❌ fail

### 1.1 Quota & Forecast
Verified by `scripts/test-forecast-service.js` → **10 pass / 0 fail**.
- ✅ Method selection: nearest-year when quota deviation ≤5%, else weighted-average decay (w=0.7^i) — asserted (B1/B2).
- ✅ Nearest-year picks the *closest matching* year within ±5% (B2).
- ✅ All scenarios + recommended returned by `computeAllScenarios` (4 scenarios incl. optimistic/base/pessimistic + recommended).
- ✅ Occupancy warnings fire at >95% and <60% (B4).
- ✅ Capacity/quota read dynamically; new `enrollmentQuota` field supported.
- 🔵 NEEDS-LIVE: `/admin/quota` & `/admin/demand-forecast` page render + save-to-DB round trip (logic/routes present: `admin-quota-routes.js`, `admin-demand-forecast-routes.js`).

### 1.2 Simulation (admintest)
Verified by `scripts/run-simulation-e2e-test.js` → **ALL ASSERTIONS PASSED** (full 9-step run).
- ✅ Workspace seed/clone; cohort distribution (Y1=333, Y2=691, Y3=570, Y4+=470, Y5+=180).
- ✅ Allocation engine respects quota bands (year1 326≤326, year2 196≤196, year3 97≤97, year4+ 32≤32).
- ✅ Score-sorted waitlist, reject distribution monotonic by seniority, fill rate ≤97% (maintenance buffer).
- ✅ Manual edits (remove/promote), **apply to real allocation** (326 applied / 324 skipped, snapshot APPLIED).
- ✅ **Undo** restores snapshot to UNDONE and AllocationCycle back to PENDING.
- ✅ Report generated with quota columns + synthetic-data disclaimer.
- 🔵 NEEDS-LIVE: the apply-to-production **confirmation dialog** is a UI element (engine path verified).

### 1.3 Allocation Cycle (real)
- ✅ `allocationService.js` creates `RoomAllocation` with `status: 'ACTIVE'` (lines 286/431/625) and `allocationType: 'AUTO'`.
- ✅ `AllocationAuditLog` written by `allocationService.js` / `allocation-routes.js` / `allocation-dashboard-routes.js`.
- ✅ Manual/override allocation types exist and are distinguished: `ADMIN_APPROVAL` (pending-app approval, `admin-page-routes.js:468`), `MANUAL_OVERRIDE` (simulation apply), `REBALANCE`, `WAITLIST_PROMOTION`.
- 🔵 NEEDS-LIVE: dashboard render, cycle create/open, `POST /api/allocation/cycles/:id/run` end-to-end against a live open cycle.

### 1.4 Student Registration & Room Self-Selection
- 🔵 NEEDS-LIVE: registration portal submit, "no open cycle" message, priority-claim submit/approve UI, `/room-status` render. Routes present (`registration-routes.js`, `room-status-routes.js`, `priorityClaimRoutes.js`); priority-claim status update path exists.

### 1.5 Pending Applications
- ✅ Approval path assigns room and records `allocationType: 'ADMIN_APPROVAL'` (`admin-page-routes.js`).
- 🔵 NEEDS-LIVE: list pagination/filtering, reject notification, waitlist position UI.

### 1.6 Room Transfer
Verified by `test:features` Room-Transfer schema block (all passed) + code read of `admin-room-transfer-routes.js`:
- ✅ Assign handler removes student from **old** room occupants (`:199` filter), adds to **new** room (`:213`), updates `student.roomNumber` (`:224`), sets transfer `status='completed'` (`:232`).
- ✅ `ActivityLog` entry created on assign (`:251`) and student notified (`:246`).
- ✅ Reject requires reason; status transitions pending→rejected verified by test.
- 🔵 NEEDS-LIVE: student `/room-status` reflecting new roommates after transfer.

### 1.7 Maintenance Requests
- ✅ Schema + admin requests filtering route present; `notificationService.sendMaintenanceNotification()` wired.
- 🔵 NEEDS-LIVE: submit → email send → status transitions (submitted→in_progress→resolved) across all 10 types.

### 1.8 Notifications
- ✅ **Badge/web-list unified** on `getStudentNotifications` (used by both `notificationRoutes.js` and mobile `notification.routes.js`) — matches the prior badge-vs-list mismatch fix.
- 🔵 NEEDS-LIVE: per-event firing, mark-as-read unread-count decrement, `/api/web-notifications` payload.

### 1.9 Admin Dashboard & Other Pages
- 🔵 NEEDS-LIVE: dashboard stats, violations CRUD, cohort-shift timeline, academic-window save, enrollment-planning save, activity log view. All routes mounted and views exist.

### 1.10 Auth & Account
- 🔴 **FAIL (code bug): `POST /api/admin/emergency-reset-2fa` will crash** — `logSecurityEvent` is undefined in `index.js` (called `:233`, not imported `:28`). Same defect on `/api/admin/migrate-violations` (`:263`). See 3.3 for fix.
- ⚠️ Checklist says `/api/health`; actual health endpoints are mounted at **`/health`** (`/health`, `/health/db`, `/health/redis`, `/health/sentry`) — `/api/health` does **not** exist (would 404). Update monitoring config or the checklist.
- 🔵 NEEDS-LIVE: login/logout, 2FA TOTP/email OTP prompt, session expiry redirect.

## Phase 2 — UI/UX

> Layout/responsive/touch-target/modal-centering/loading-state checks require a rendered
> browser session and are marked 🔵 NEEDS-LIVE. The static (source-verifiable) checks are
> reported below.

### 2.1 Desktop / 2.2 Mobile (layout, responsiveness, modals, loading/empty states)
- 🔵 NEEDS-LIVE for all per-page layout/overflow/touch-target/modal/loading checks. All listed
  admin/student/public views exist and are wired to routes (except the orphans in 3.1).

### 2.3 Visual Consistency (statically verifiable)
- ✅ No `[object Object]` literals rendered in any view.
- ✅ No stray `undefined` text leaks in views (only a JS `parseInt(...) : undefined` expression).
- ✅ Date locale consistent: **110× `vi-VN`**, **0× `en-US`/`en-GB`** → `dd/mm/yyyy` everywhere.
- ⚠️ **Brand color is split across the app.** Admin views use red `#C8102E` (129 occurrences);
  the premium public/student shell uses copper `#c08157` (8 occurrences, in
  `student/profile.ejs`, `student/service-requests.ejs`, `public/css/premium-home.css`,
  `public/css/student-premium-shell.css`). Two different "primary" brand colors coexist
  depending on area. Decide on one source of truth (the project's memory records copper
  `#c08157` as the intended brand) and reconcile, or document the two-palette split as intentional.
- 🔵 NEEDS-LIVE: Font Awesome icon rendering, badge color coding parity, card/shadow consistency.

### 2.4 UX Flows (statically verifiable)
- 🔴 **Broken link:** `/student/explore-rooms` is linked from `student/my-ranking.ejs:485`
  and `student/room-360-viewer.ejs:267`, but **no route serves it** → navigating there 404s.
  Either add the route (rendering `student/explore-rooms.ejs`, currently orphan) or fix the links.
- 🔵 NEEDS-LIVE: breadcrumb/back-nav presence, inline validation, confirmation dialogs on
  destructive actions, post-POST feedback, pagination, search/filter correctness.

## Issues Requiring Manual Follow-up
_(compiled at end)_

## Notes
_(compiled at end)_
